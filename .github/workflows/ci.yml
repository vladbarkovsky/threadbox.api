name: CI
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: self-hosted
    defaults:
      run:
        working-directory: ThreadboxApi
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: Check PATH
      run: echo $PATH
    - name: Restore
      run: dotnet restore
    - name: Build
      run: dotnet build --no-restore -c Release
  docker:
    runs-on: self-hosted
    needs: build
    defaults:
      run:
        working-directory: ThreadboxApi
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: Build Docker image
      run: docker build -t threadbox-api:${{ github.sha }} .
