services:
  postgres:
    image: postgres:18
    restart: unless-stopped
    environment:
      POSTGRES_USER: threadbox_api
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql
  threadbox-api:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      ASPNETCORE_ENVIRONMENT: Release
      ASPNETCORE_URLS: http://+:8080
      CONNECTION_STRINGS__POSTGRES: ${CONNECTION_STRINGS__POSTGRES}
      DEFAULT_ADMIN_CREDENTIALS__PASSWORD: ${DEFAULT_ADMIN_CREDENTIALS__PASSWORD}
      OIDC_BFF_CLIENT_SECRET: ${OIDC_BFF_CLIENT_SECRET}
      SSL_PASSWORD: ${SSL_PASSWORD}
    expose:
      - "8080"
    volumes:
      - /etc/threadbox/certs:/certs:ro
    depends_on:
      - postgres
  nginx:
    image: nginx:alpine
    restart: unless-stopped
    ports:
      - "5000:5000"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - /etc/threadbox/certs:/certs:ro
    depends_on:
      - threadbox-api
volumes:
  pgdata:
